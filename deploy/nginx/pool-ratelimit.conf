# Shared rate-limit zone for the dashboard vhost. Lives in conf.d/ so it
# is loaded inside the top-level http{} block before the vhost is
# processed from sites-enabled/.
limit_req_zone $binary_remote_addr zone=pool_dash:10m rate=30r/s;
